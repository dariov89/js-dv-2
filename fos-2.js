// El objeto MATH

/*

Math es como un contenedor de herramientas y métodos para operaciones matemáticas. Estos son algunos de sus métodos:

-MIN MAX para mínimos y máximos
-CEIL FLOOR ROUND para redondeos o truncamientos
-SQUARE ROOT para raíz cuadrada
-RANDOM para números al azar

*/


// La clase DATE

/*

La clase Date nos permite instanciar un nuevo objeto que nos genera fecha y hora actual. Al tratarse de una clase constructora, utilizamos "new"

Ej:

let fechaDeHoy = new Date();

Al ser una clase, su constructor puede recibir distintos parámetros y de acuerdo a ello, ir devolviéndonos una u otra respuesta según ingresemos un tipo y otro tipo de parámetro.

También posee métodos para obtener un valor singular como getMonth, getFullYear y getDay que nos dan el mes, año y día respectivamente del objeto.

Otros métodos como toDateString, toLocaleString, etc. nos dan las fechas en distinto formato según se requiera.


2nda pre entrega

estructura HTML
variables JS necesarias
funciones esenciales del proceso a simular
objetos de JS
arrays
metodos de busqueda y filtrado sobre el array


No les logré armar ahí mismo el resumen de la preentrega pero voy a dejarles algo escrito acá en el chat gente:



Debe ser un SIMULADOR INTERACTIVO. Es decir, toda funcionalidad que se le agregue tiene que efectuarse solo de acuerdo a lo que solicita el usuario. Ejemplo: si ponen un array literal y aplican un filter sin que lo solicite el usuario, eso NO ES un simulador interactivo y por ende no es algo válido.
Deben incorporar arrays, objetos y métodos de array para filtrado, búsqueda, transformación o lo que se requiera. No hace falta usar todos los métodos, solo algunos mientras el criterio esté bien y los apliquen de manera correcta.
Recuerden que el uso de VAR está penado por ley (? así que es motivo de desaprobar el trabajo.
Los que tengan deploy hecho, pueden adjuntar el deploy si quieren, pero no dejen de adjuntar el repositorio también porque los tutores además de chequear funcionalidad también leen código.
Cualquier duda comuníquense con sus tutores si tienen alguna complicación o algo, no intenten presentar trabajos copiados que ya han habido algunos y los detectamos.
No dejen de utilizar CoderAsk para consultas de contenido ya que esos son los tutores encargados de resolverles sus dudas académicas (Ni Diego, ni Damián ni yo tenemos esa obligación más allá que podamos hacerlo).
Para esta entrega NO ES OBLIGATORIO EL USO DE DOM, pero si quieren agregarlo, comuníquense con sus tutores para que les den el visto bueno.
Recuerden que todo lo que puedan poner en el html no va a la nota. La nota depende de lo que hagan en su archivo JS, por ende priorícenlo

*/